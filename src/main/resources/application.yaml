spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/music_repository_api
    username: postgres
    password: bnxpost
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  main:
    allow-circular-references: true

  servlet:
    multipart:
      max-request-size: -1
      max-file-size: -1

  mvc:
    hiddenmethod:
      filter:
        enabled: true

storageLocation: music
